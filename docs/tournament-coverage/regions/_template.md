# <Region>

Copy this file to `<region-slug>.md` when starting a new region. Fill the
numbers from the pipeline in [../README.md](../README.md). Delete this line.

Region tag values in `golf_clubs.region` for this region (check for
Regierungsbezirk / city aliases the way Bayern has them):

## Where it stands (YYYY-MM-DD)

| | start | after this work |
|---|---|---|
| clubs (raw rows) | | |
| clubs with tournaments | | |
| clubs still empty | | |

## What did the work

- Merge:
- PC CADDIE ids:
- PDF calendars:

## Still empty, by `tournament_platform`

- `unknown`:
- `albatros`:
- image-only / stale / dead:

## Manual fixes applied

(URLs corrected by hand, clubs needing per-course attribution, anything a future
run should not clobber.)
